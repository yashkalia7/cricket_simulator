{
  "name": "@cricket/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Ontology, geometry, rules engine, scenario model, getSuggestions. Pure TypeScript, zero runtime dependencies beyond zod.",
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --max-warnings 0"
  },
  "dependencies": {
    "zod": "^4"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3"
  }
}
